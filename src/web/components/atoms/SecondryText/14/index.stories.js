import React from "react";
import { storiesOf } from "@storybook/react";
import PrimaryText16 from ".";

storiesOf("PrimaryText16", module).add("default", () => (
  <PrimaryText16 text="PrimaryText16" />
));
