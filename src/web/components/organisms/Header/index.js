import React from "react";
// import PropTypes from 'prop-types'

// import { Title } from '../../molecules/Title'

/**
 * Header
 * @param {*}
 */
const Header = () => (
  <header className="c-appbar">
    <h1 className="c-title fc-primary">RetailMarketing</h1>
  </header>
);

export default Header;
